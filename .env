# MongoDB connection string (provided by Railway when you add the MongoDB plugin)
MONGO_URL=mongodb+srv://<username>:<password>@cluster0.mongodb.net/quizapp?retryWrites=true&w=majority

# Port your server runs on (Railway auto-assigns this if not set)
PORT=3000

# Secret key for session handling or other crypto use
SESSION_SECRET=supersecurequizkey123

# Optional: allow CORS for frontend if hosted separately
CORS_ORIGIN=https://yourfrontendurl.com
